{
	"$schema": "https://turbo.build/schema.json",
	"pipeline": {
		"build:next": {
			"dependsOn": ["db:deploy"],
			"outputs": [".next/**", "!.next/cache/**"]
		},
		"db:deploy": {
			"dependsOn": ["db:generate"]
		}
	}
}
